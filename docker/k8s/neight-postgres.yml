apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: neight
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: neightec-postgres
  namespace: neight
spec:
  replicas: 1
  serviceName: neightec-postgres
  selector:
    matchLabels:
      app: neightec-postgres
  template:
    metadata:
      labels:
        app: neightec-postgres
    spec:
      containers:
        - name: neightec-postgres
          image: bitnami/postgresql:11.7.0
          env:
            - name: POSTGRESQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-password
                  key: password
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-username
                  key: username
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: postgres-persistent-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-persistent-storage
          persistentVolumeClaim:
            claimName: postgres-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: neightec-postgres
  namespace: neight
spec:
  selector:
    app: neightec-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: NodePort